
## v0.2.0 迁移python后端、替换MySQL为mongodb

1. 将原后端go代码接口全部迁移到python代码
2. 删除所有go代码
3. 后端存储数据库修改为Mongodb


## v0.3.0 扩充模型配置相关

```text
现在在系统配置中，只支持阿里云的大模型配置，需要进行以下的几个功能改造：
1.我希望能够配置很多不同的模型（不同厂商的）
2.在能够支持配置多个模型的情况下， 在对提示词进行测试的时候，以及AI优化部分，都应该可以选择使用不同的模型来处理
现在需要你改造对应的前后端功能
```

优化效果：

1. 支持配置添加多个模型
2. 支持在测试时选择不同的模型进行测试
3. AI优化部分暂时通过全局默认模型来进行优化


## v0.4.0 优化页面跳转

```text
系统当前在功能使用方面有几个问题，涉及到前端页面功能的调整：
1.当我选择项目，进一步选择prompt 提示词之后，如果我在选择其他的版本， 这个时候我选择返回上一页的时候，会回到上一个版本。 在这个地方似乎没有回到首页，或者回到项目的功能，交互上不太友好，需要优化一下
```

优化效果：

1. 现在进入到不同版本，选择返回上一页的时候，也会返回到项目页面

## v0.5.0 修复刷新页面后无法加载问题

## v0.6.0 增加测试历史展示功能

```text
需要在系统的prompt测试页面增加对于测试历史的记录， 并支持测试历史的标题编辑、记录删除的功能，测试历史页面放在页面的左侧，请完成前后端代码功能
```

优化效果：
1. 增加测试历史